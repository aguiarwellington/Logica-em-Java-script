
/**Create variables to store the balance, deposit, and withdrawal values of a bank account. 
 * Then perform addition and subtraction operations between them using a variable called operation, 
 * considering the function of each variable created previously. Display the final results in the console.
 * 
 */


var balance = 500;
var deposit = 600;
var withdrawal = 300;

var operation; 


operation = balance + deposit;

console.log(operation);

operation = operation - withdrawal;

console.log(operation);